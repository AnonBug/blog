.theme-container.loaf-page {
    .page {
        h2 {
            color: #3eaf7c;
        }

        img {
            border: 5px solid #cccccc;
            border-radius: 10px;
        }
    }
}

.theme-default-content code {
    background-color: rgba(62, 175, 124, 0.1);
    color: inherit;
    font-size: 0.95em;
}